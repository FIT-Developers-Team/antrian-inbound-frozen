{
  "name": "antrian-inbound-frozen",
  "private": true,
  "version": "2.0.0",
  "description": "Antrian Inbound Frozen - papan antrean inbound gudang beku",
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.2",
    "@capacitor/core": "^8.4.2"
  },
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "doctor": "node scripts/doctor.mjs",
    "push": "node scripts/autopush.mjs",
    "check": "node --check js/app.js && node --check js/api.js && node --check js/config.js && node --check js/format.js && node --check js/sla.js && node --check js/store.js && node --check js/theme.js && node --check js/ui.js && node --check js/pages/board.js && node --check js/pages/register.js && node --check js/pages/report.js && node --check js/pages/settings.js && node --check js/deployment.js && node --check api/server.mjs && node --check api/sync-superset.mjs",
    "test": "node --test test/*.test.js",
    "android:sync": "npx cap sync android",
    "android:build:debug": "cd android && gradlew.bat assembleDebug",
    "stack:up": "docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build",
    "stack:down": "docker compose -f docker-compose.yml -f docker-compose.local.yml down",
    "stack:logs": "docker compose -f docker-compose.yml -f docker-compose.local.yml logs -f api"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^8.4.2",
    "esbuild": "^0.28.1",
    "supabase": "^2.39.2"
  }
}
